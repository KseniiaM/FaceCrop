<?xml version="1.0" encoding="utf-8" ?>
<views:MvxContentPage
             xmlns:views="clr-namespace:MvvmCross.Forms.Views;assembly=MvvmCross.Forms"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:controls="clr-namespace:FaceCrop.CustomControls"
             x:Class="FaceCrop.Views.DisplayImagesPage">
    <ContentPage.Content>
        <Grid Margin="10"
              VerticalOptions="FillAndExpand" 
              HorizontalOptions="FillAndExpand">

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <controls:CustomImageView
                       Grid.Row="0"
                       Grid.ColumnSpan="2"
                       Source="{Binding SelectedFaces, Mode=TwoWay}"
                       Aspect="AspectFit"
                       SelectedRectangle="{Binding SelectedRectangle, Mode=TwoWay}"
                       RectangleCollection="{Binding Rectangles}"
                       IsVisible="{Binding Source={x:Reference croppedImage}, 
                                           Path=IsVisible, 
                                           Converter={StaticResource InverseBoolConverter}, 
                                           FallbackValue=false}"/>
            
            <Image 
                x:Name="croppedImage"
                Grid.Row="0"
                Grid.ColumnSpan="2"
                Aspect="AspectFit"
                Source="{Binding CroppedImage}"
                IsVisible="{Binding CroppedImage, Converter={StaticResource ValueNotNullToBoolConverter}}"/>
            
            <Button
                x:Name="RefreshButton"
                Grid.Row="1"
                Grid.Column="0"
                VerticalOptions="StartAndExpand"
                HorizontalOptions="FillAndExpand"
                Text="Reset"
                Style="{StaticResource ButtonStyle}"
                BackgroundColor="{StaticResource LightBlueColor}"
                Command="{Binding RefreshSelectionCommand}"/>

            <Button
                x:Name="CropButton"
                Grid.Row="1"
                Grid.Column="1"
                VerticalOptions="StartAndExpand"
                HorizontalOptions="FillAndExpand"
                Text="Crop Selected"
                Style="{StaticResource ButtonStyle}"
                BackgroundColor="{StaticResource LightBlueColor}"
                Command="{Binding CropCommand}"/>
            
        </Grid>
    </ContentPage.Content>
</views:MvxContentPage>